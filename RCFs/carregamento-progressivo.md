<!-- AI-PROCESSED -->
# RCF-JCEM-CARREGAMENTO-PROGRESSIVO-001

Status: vigente.

Escopo: carregamento inicial, loader global, recursos pesados, skeleton loading e indicadores de progresso do blog.

## Regras Normativas

- Toda imagem editorial de conteúdo elegível em mídia de tela DEVE receber, por aprimoramento progressivo, um controle sutil de ampliação sem reflow. O controle DEVE ter alvo adequado a mouse e toque, nome acessível e acionamento por teclado; PODE surgir por `hover`, foco ou primeira interação de toque/clique, mas NÃO PODE depender exclusivamente de `hover`. [c35b277]
- A visualização ampliada DEVE usar o asset de maior qualidade já disponível no elemento, preservar proporção, limitar-se à viewport, fornecer fechamento inequívoco por controle, `Escape` e retorno ao contexto/foco anterior. Fullscreen nativo PODE ser usado, com fallback modal local quando indisponível ou recusado. [de992a9]
- Logo, ícone, avatar, imagem de controle, card/thumbnail e mídia pertencente a componente com contrato próprio NÃO são imagem editorial elegível. A exclusão DEVE decorrer do papel semântico/estrutural, não de exceção arbitrária por URL, página, formato ou dispositivo. [de992a9]
- O aprimoramento de ampliação DEVE permanecer ausente da impressão e não PODE alterar o asset, suas dimensões no fluxo, `srcset`, `sizes`, skeleton, legenda, link editorial ou comportamento de carregamento. [de992a9]
- Imagem destacada ampla limitada pela viewport DEVE preencher a altura visual calculada sem deformação: o contêiner e a caixa da imagem DEVEM compartilhar a mesma altura efetiva, enquanto a largura deriva da proporção intrínseca e permanece centralizada e limitada à viewport. `aspect-ratio` de reserva de carregamento NÃO PODE conservar altura excedente depois que esse limite passa a governar a imagem. [54a9a7d]

## Covers editoriais e composição wide

- A arquitetura preserva modalidades cumulativamente distintas: o hero legado sem estilo explícito; a cover comum `content`, alinhada à zona visível do artigo; a cover horizontalmente infinita `wide` de uma imagem; e a variante opcional `wide` tripla. Ausência de estilo NÃO PODE ativar `wide`, e regra de uma modalidade NÃO PODE vazar para outra. [5db4d00]
- `featured_image_style: content` ativa exclusivamente a cover comum; o alias legado `inline` converge ao mesmo modo. Sua caixa DEVE coincidir com as bordas visíveis da zona do artigo, sem `100vw`, margem de sangria ou superfície periférica de página, preservando proporção, enquadramento e conteúdo integral. [5db4d00]
- `featured_image_style: wide` ativa exclusivamente a composição horizontalmente infinita; os aliases legados `full`, `full-width` e `bleed` convergem ao mesmo modo. Somente essa modalidade PODE usar largura de viewport e sangria lateral, segundo o enquadramento próprio de `single` ou a composição própria de `triptych`. [32cb5f8]
- A altura mínima, a altura preferencial e a altura máxima das modalidades `content` e `wide` DEVEM derivar do mesmo token responsivo vigente, com componente relativo à viewport. CSS, include, front matter e script NÃO PODEM duplicar valor numérico concorrente; rotação e resize DEVEM recalcular a mesma regra sem salto ou estado intermediário persistente. [5db4d00]
- No modo `content`, a caixa e toda superfície visível da cover DEVEM terminar precisamente nas bordas visíveis da zona do artigo. A mídia central permanece contida nessa caixa; quando proporção e limites de altura impedirem a mídia de tocar simultaneamente os quatro lados, a área local remanescente preserva o fundo normal do componente, sem imagem duplicada, blur, preenchimento artificial, `100vw`, sangria ou recorte destrutivo. [32cb5f8]
- Flag associada à publicação pertence à camada acima da cover e DEVE permanecer integralmente visível em `content` e `wide`. Cover, stage, skeleton, `overflow`, contenção ou novo stacking context NÃO PODEM cobri-la nem recortá-la; o recorte necessário da mídia DEVE ocorrer em descendente ou irmão que não contenha a flag. [32cb5f8]
- Cover canônica para compartilhamento wide usa `1200×630` quando o contrato editorial permitir. Fonte com outra proporção DEVE ser preservada como original e gerar derivado novo; simples redimensionamento só é válido quando não distorcer nem degradar. Extensão generativa autorizada DEVE limitar-se às bordas necessárias, conservar o conteúdo central e registrar ferramenta, entrada, hash, parâmetros, saída e revisão visual, sem sobrescrever o original. [e983edf]
- O modo `wide` de uma imagem, selecionado por ausência de `header.image_wide_mode` ou por `single`, usa exclusivamente `header.image`, mantém a caixa na altura resultante do token responsivo e alinha o centro intrínseco da imagem ao centro do viewport. A mídia DEVE manter essa altura, derivar a largura de sua proporção e permitir que o excesso horizontal seja recortado naturalmente e de modo simétrico pela caixa de viewport; `max-width`, `contain`, fit de largura, redução da altura para revelar toda a largura, imagem duplicada, background, blur ou outra superfície periférica artificial são proibidos nesse modo. [32cb5f8]
- O modo `wide` triplo usa `header.image` como `central`, `header.image_wide_left` como segmento repetível à esquerda e `header.image_wide_right` como segmento repetível à direita, ativado por `header.image_wide_mode: triptych`. Esses campos são especializados de `wide` e NÃO PODEM converter `content` ou o hero legado em composição infinita. Ausência das duas laterais resolve para `single`; combinação parcial, proporção incompatível ou bordas não conciliáveis DEVE falhar no build. [5db4d00]
- Segmentos laterais DEVEM compartilhar altura e escala efetivas com a central. A borda direita de `left` coincide pixel a pixel com a borda esquerda de `central`, e a borda esquerda de `right` com a borda direita de `central`; repetição externa respeita sua direção sem espelhamento implícito. [e983edf]
- O hero legado sem estilo explícito preserva largura integral e proporção natural enquanto a altura projetada da mídia couber na área realmente disponível do primeiro viewport com `scroll = 0`, descontados o cabeçalho e os elementos efetivamente anteriores à cover. Se essa projeção ultrapassar a área disponível, o mesmo hero DEVE adotar, antes da exposição estável ao usuário, a geometria da zona do artigo e as regras de contenção do modo `content`, sem compressão arbitrária, alteração do asset ou conversão editorial da modalidade. [32cb5f8]
- A decisão responsiva do hero legado DEVE usar dimensões intrínsecas e medições de layout estabilizadas, manter limiar determinístico e agrupar leituras e escritas por frame ou evento consolidado. Inicialização, fontes, resize, rotação e mudança de viewport visual PODEM disparar nova decisão; scroll ordinário NÃO PODE dispará-la nem ser lido como critério. A implementação NÃO PODE mover o scroll, alternar repetidamente entre estados, produzir flicker perceptível, executar ciclo contínuo de reflow ou conservar estado incompatível após estabilização. [32cb5f8]
- `header.og_image` e derivados sociais NÃO substituem a mídia editorial visível de `content`, `wide single` ou `wide triptych`; a página individual conserva `header.image`, e somente os metadados sociais consomem o derivado aplicável.
- O layout e a implementação DEVEM preservar a modalidade já declarada por cada publicação. Correção localizada de largura, altura, flag, asset ou metadado NÃO autoriza reclassificar post, preencher estilo ausente com `wide`, criar default global infinito nem remover modalidade preexistente. [5db4d00]

### Configuração, geometria e modos estendidos de COVER

- O schema versionado `config/cover-system.json` DEVE centralizar proporção útil `1200/630`, defaults, aliases, modos, patterns e zonas de Hero; a resolução DEVE seguir configuração global → definição do modo → front matter da página, sem inferir campo ausente nem mutar o Markdown. [PENDENTE-CODIGO]
- A configuração nova `cover` DEVE coexistir com `featured_image`, `featured_image_style` e `header.*`; os campos legados conservam significado e precedência documentados, enquanto `cover` somente especializa propriedades explicitamente declaradas. [PENDENTE-CODIGO]
- A zona do artigo e a zona da janela DEVEM permanecer coordenadas distintas. A área útil central da COVER usa proporção canônica `1200:630` (aproximadamente `1,91:1`), alinha-se à zona aplicável e NÃO PODE incluir excedente lateral decorativo, pattern ou crop. [PENDENTE-CODIGO]
- O modo `wide single` DEVE preencher a largura da janela com uma única mídia centralizada, ajustada pela altura e com largura automática; a região útil central preenche a altura, e somente o excedente horizontal pode ser recortado simetricamente, sem scroll próprio da COVER. [PENDENTE-CODIGO]
- O modo `wide triptych` DEVE compor `pattern esquerdo + central + pattern direito`, manter a central `1200:630` alinhada à zona do artigo e impedir gap, sobreposição, scroll ou emenda visível. Cada pattern lateral PODE ser imagem, cor hexadecimal ou `linear-gradient(...)` validado; valor parcial, inseguro ou inválido falha no build. [PENDENTE-CODIGO]
- `FullWindow`, `windowHeight`, `windowWidth`, `innerFullWindow`, `innerWindowHeight` e `innerWindowWidth` DEVEM resolver para identificadores canônicos sem criar modalidade redundante nem alterar os aliases legados de `wide`. [PENDENTE-CODIGO]
- `FullWindow` externo DEVE preencher a janela com proporção preservada e escolher exatamente uma dimensão governante conforme a razão disponível; `windowHeight` governa pela altura e largura automática; `windowWidth` governa pela largura e altura automática, sem ultrapassar a altura disponível. [PENDENTE-CODIGO]
- Modos `inner*` DEVEM usar a área restante abaixo da masthead integral como viewport própria, conservar a mesma semântica de fit de seus pares externos e NÃO PODEM aplicar transparência especial ao cabeçalho. [PENDENTE-CODIGO]
- Modos externos DEVEM iniciar atrás da masthead, cuja opacidade inicial resolve para `0,30` salvo override válido de página; após o limiar real de scroll, a masthead torna-se sólida. Esse estado NÃO PODE contaminar página sem modo externo, impressão ou fallback sem JavaScript. [PENDENTE-CODIGO]
- Resize, rotação, mudança de viewport visual e carregamento de fonte DEVEM recalcular a geometria sem deslocar scroll, criar loop, alternância instável, overflow horizontal ou dimensão superior à viewport governante. [PENDENTE-CODIGO]

### Hero editorial comum a todas as COVERs

- Hero editorial é uma camada opcional e ortogonal ao modo de COVER; a ausência de Hero preserva exatamente a mídia, geometria, flag, barras, legenda, skeleton e comportamento anterior.
- Conteúdo do Hero DEVE nascer de Markdown declarativo no front matter, aceitar múltiplos parágrafos e preservar HTML sanitizado, headings e links gerados pelo pipeline vigente; CTA opcional exige simultaneamente rótulo e destino válidos. [PENDENTE-CODIGO]
- O Hero DEVE ocupar somente a área útil central da COVER, nunca patterns, crop ou excedentes laterais, e oferecer seis zonas canônicas: `top-left`, `top-right`, `bottom-left`, `bottom-right`, `center` e `full`. [PENDENTE-CODIGO]
- As zonas de canto DEVEM limitar largura e altura do bloco sem fazê-lo crescer para uma lateral inteira; `center` usa padding substancial e `full` usa padding reduzido. Zona, alinhamento horizontal, alinhamento vertical, largura máxima e tratamento de fundo DEVEM ser propriedades independentes. [PENDENTE-CODIGO]
- Texto curto ou longo, link e CTA DEVEM manter contraste e legibilidade por tema e imagem mediante proteção declarada e previsível; a proteção NÃO PODE apagar a COVER, ocupar área decorativa nem depender de biblioteca externa. [PENDENTE-CODIGO]
- Hero DEVE preservar ordem DOM, nome de link, foco visível, toque, zoom, reduced motion, 320 px e leitura sem JavaScript; nenhuma disposição visual PODE alterar a ordem semântica do conteúdo. [PENDENTE-CODIGO]

- HTML e CSS devem produzir conteúdo legível imediatamente; JavaScript, consentimento e aprimoramentos progressivos NÃO PODEM ocultar ou bloquear a primeira renderização visível da página. [00251f5]
- Recursos essenciais são HTML, CSS, JavaScript próprio necessário à inicialização e dependências leves do JavaScript, como JSON, XML ou formatos equivalentes.
- Imagens, `background-image`, vídeos, áudios, iframes, fontes opcionais e demais assets pesados não devem bloquear a liberação inicial da página. [860dfdf]
- Recursos pesados devem carregar de forma assíncrona, progressiva e tolerante a falhas. [860dfdf]
- Todo componente elegível que dependa de asset potencialmente lento deve exibir automaticamente skeleton loading em CSS puro até que o asset esteja carregado ou falhe. [860dfdf]
- Componentes elegíveis incluem banners, imagens destacadas, cards, thumbnails, galerias, backgrounds visuais e componentes opt-in com `data-jcem-skeleton`.
- Skeleton loading deve preservar o espaço do componente no fluxo sempre que a geometria do componente for conhecida por CSS ou marcação. [860dfdf]
- A base visual do skeleton deve permanecer visível durante todo o ciclo de animação; somente o brilho de varredura pode se deslocar. [860dfdf]
- O brilho de varredura deve permanecer em camada visual própria e perceptível sobre o espaço reservado do componente, sem depender da visibilidade do asset final. [860dfdf]
- O contêiner do skeleton deve possuir textura ou padrão visual próprio enquanto estiver em `loading`, para que o placeholder permaneça reconhecível mesmo se pseudo-elementos falharem ou forem sobrepostos. [860dfdf]
- Quando o recurso estava pendente, o skeleton deve permanecer visível por janela mínima perceptível antes da transição para o conteúdo final. [860dfdf]
- Metadados de assets constituem camada opcional de otimização; o tema jamais deve depender deles para funcionar corretamente. [860dfdf]
- Quando disponíveis, metadados de assets devem ser usados para otimizar desempenho percebido, responsividade, reserva antecipada de espaço, estruturação automática de componentes e precisão do skeleton loading. [860dfdf]
- Imagens externas elegíveis devem possuir metadados declarativos em `_data/jcem_asset_metadata.yml` ou fonte equivalente consumida no build; cards e imagens destacadas não podem depender do carregamento da imagem para definir a altura final. [860dfdf]
- A primeira renderização de cards com imagem deve emitir `width`, `height` e/ou `aspect-ratio` suficientes para impedir expansão, contração ou reposicionamento após o carregamento do asset. [860dfdf]
- O skeleton de imagens deve combinar padrão/textura fixa com brilho de varredura esquerda-para-direita em camada própria, com contraste ajustado por tema. [860dfdf]
- A geração de metadados de assets deve ocorrer automaticamente durante o build. [860dfdf]
- A geração deve ser incremental sempre que tecnicamente viável, evitando reprocessamento de assets não alterados. [860dfdf]
- O índice consolidado de metadados deve ser cacheável, possuir baixa latência e minimizar requisições HTTP. [860dfdf]
- O índice DEVE registrar variantes responsivas conhecidas por asset, com URL, largura, altura, proporção, formato e custo em bytes quando disponível. [860dfdf]
- Cards e thumbnails DEVEM emitir `srcset` e `sizes` ou contrato equivalente que permita ao navegador escolher a menor variante suficiente para largura renderizada, viewport e DPR; a maior imagem NÃO DEVE ser o default por conveniência. Imagem destacada ou interna do artigo preserva o original, salvo autorização editorial explícita diversa. [2f061ba]
- Imagem destacada de artigo DEVE preservar proporção e qualidade e obedecer ao limite vertical próprio de sua modalidade. Em mídia de tela, `content` e `wide` usam o token de altura; o hero legado alterna entre largura integral e geometria do artigo pelo contrato do primeiro viewport. A contenção NÃO PODE alterar 404, `noscript`, cards, impressão ou o asset original. [32cb5f8]
- Variantes de cards e thumbnails DEVEM nascer diretamente do original declarado uma única vez. Configuração e índice DEVEM registrar hash SHA-256, tamanho e commit do original; build subsequente apenas valida e reutiliza as saídas, sem recompressão cumulativa. [2f061ba]
- Variante incompatível em proporção ou finalidade NÃO DEVE integrar o mesmo conjunto. Ausência de variantes DEVE preservar a origem como fallback sem inventar arquivo ou URL. [860dfdf]
- Card criado no cliente DEVE consumir o mesmo índice e regra de seleção do HTML estático, sem duplicar heurística divergente. [860dfdf]
- Metadados incorporados diretamente ao arquivo original, como EXIF ou mecanismo equivalente, só devem ser gravados quando houver suporte seguro, preservação integral dos metadados existentes e ausência de impacto relevante no build. [860dfdf]
- A substituição do skeleton pelo conteúdo definitivo deve usar transição suave, sem flickering perceptível. [860dfdf]
- A implementação não deve introduzir mudança visual deliberada na identidade do componente carregado. [860dfdf]
- Indicadores de carregamento e progresso devem possuir contraste suficiente durante toda a exibição, inclusive em tema claro, tema escuro, telas de baixo brilho e conexões lentas. [860dfdf]
- Estas regras constituem o comportamento padrão para componentes atuais e futuros, salvo justificativa técnica explícita registrada no ponto de implementação.

## Assets editoriais e metadados sociais

- Draft ou publicação NÃO PODE depender diretamente de `web.archive.org` para entregar imagem, SVG, áudio, vídeo, PDF incorporado ou outro asset editorial. O pipeline DEVE resolver primeiro cópia local inequivocamente equivalente por origem, nome, conteúdo e hash; download só ocorre diante de ausência comprovada. URL de provenance em `source:` e hyperlink citacional para documento arquivado não constituem asset e permanecem preservados. [e983edf]
- `_site/` é artefato gerado, nunca origem editorial. Asset encontrado somente nessa árvore DEVE ser promovido ao namespace-fonte aplicável com hash e vínculo documental antes de qualquer novo build; a cópia gerada isolada não satisfaz preservação nem rastreabilidade. [e983edf]
- `header.image` é a fonte central visível e, por padrão, a fonte da OG wide. `header.image_square` declara fonte quadrada opcional. `cover.og.wide_source` e `cover.og.square_source` PODEM substituir somente as respectivas fontes sociais; o build projeta derivados finais em `header.og_image` e `header.og_image_square`, sem exigir que autoria Markdown informe paths gerados nem trocar a mídia visível. [PENDENTE-CODIGO]
- A projeção Open Graph DEVE emitir primeiro a imagem wide `1200×630`, seguida opcionalmente da square `400×400`, cada qual com URL absoluta HTTPS, tipo, largura, altura e alternativa. O protocolo admite múltiplos `og:image` e prefere o primeiro em conflitos; portanto a presença da square amplia opções, mas NÃO PODE ser declarada como garantia de seleção específica por WhatsApp, Instagram, Threads, LinkedIn, Facebook ou outro consumidor. [e983edf]
- X/Twitter DEVE receber `twitter:card` e `twitter:image` coerentes com o modo declarado. `summary_large_image` usa a wide; `summary` PODE usar a square quando existente e regride para a wide quando ausente. Tipo de card não PODE ser inferido por nome de plataforma nem divergir da imagem efetivamente emitida. [e983edf]
- Imagem square DEVE preservar conteúdo essencial, identidade, paleta e legibilidade da cover. Crop simples só é permitido quando não remover elemento relevante; extensão assistida segue a mesma provenance e revisão da wide. Post sem cover não recebe imagem inventada por este contrato. [e983edf]
- A imagem central do modo wide triplo é a única fonte wide de OG; segmentos laterais são composição de tela e NÃO PODEM ser concatenados ao metadado social. [e983edf]
- Covers raster compartilhadas da 404 e do fallback `noscript` DEVEM usar derivado **WebP**, não WebM, gerado uma única vez pelo allowlist vigente e com original preservado. O HTML DEVE apontar corretamente ao derivado e conservar fallback funcional quando exigido pelo componente. [e983edf]

## Implementação

- `assets/jcem/ts/site.ts` acopla o controle de ampliação às imagens editoriais elegíveis depois da primeira pintura, reutiliza o contrato de fullscreen com fallback local e preserva o foco de origem. `_sass/minimal-mistakes/skins/_variables-custom.scss` contém somente a aparência de tela do controle e da superfície ampliada.
- `_includes/head/custom.html` define o loader inicial e a barra superior com contraste próprio, independente do tema ativo; o loader sinaliza aprimoramento pendente sem encobrir o conteúdo já pintável.
- Raster JPG, JPEG ou PNG compartilhado de tema/infraestrutura, ou abrangido pela exceção de cards e thumbnails, PODE possuir derivado WebP desde que o original permaneça versionado e intacto. Cada derivado DEVE ser criado uma única vez diretamente do original e vinculado em manifesto a hash SHA-256, tamanho, mtime de origem, hash/tamanho de destino e instante de geração; mudança de origem ou destino DEVE falhar até autorização/atualização explícita, vedada qualquer cadeia de recompressão. [81772fa]
- A conversão automática DEVE operar somente sobre allowlist de fontes compartilhadas autorizadas. Asset ou ligação editorial específica NÃO PODE entrar nesse fluxo por mera existência sob `assets/`; acervos de `_drafts`, recuperação e legado também permanecem excluídos até autorização explícita aplicável. [81772fa]
- `assets/jcem/ts/site.ts` libera a página após `DOMContentLoaded` e preparação leve dos fragmentos essenciais, sem aguardar `window.load`.
- `assets/jcem/ts/site.ts` monitora imagens e backgrounds elegíveis, aplicando estados `loading`, `loaded` e `error` em `.jcem-skeleton`.
- `assets/jcem/ts/site.ts` decide o fallback responsivo dos heroes legados elegíveis por aprimoramento progressivo, com medição consolidada e sem observação de scroll. O HTML e o CSS preservam fallback legível quando JavaScript estiver ausente ou falhar.
- `_includes/archive-single.html` e `_includes/jcem/post-featured-image.html` marcam cards e imagens destacadas com skeleton server-side; somente o primeiro consome variantes responsivas editoriais.
- `_plugins/jcem_asset_metadata.rb` gera metadados opcionais de imagens, mantém cache incremental em `.jekyll-cache/jcem-asset-metadata.json` e publica índice consolidado em `assets/jcem/asset-metadata.json`.
- Metadados Open Graph e equivalentes DEVEM consumir variante social local gerada no build com altura final de 630 px e largura proporcional. O gerador DEVE partir da fonte original declarada, comparar JPEG e PNG produzidos com parâmetros estáveis, selecionar a menor saída visualmente compatível, registrar hashes, tamanho, estado temporal e parâmetros, e regenerar somente quando fonte ou parâmetros mudarem. `_plugins/jcem_social_images.rb` é o conector exclusivo entre esse manifesto e `header.og_image`; o asset editorial permanece inalterado. [81772fa]
- `_includes/archive-single.html`, `_includes/jcem/post-featured-image.html` e `recent-posts.json` usam metadados disponíveis para emitir `width`, `height` e proporção sem criar dependência funcional.
- Os consumidores de card/thumbnail DEVEM propagar variantes, `srcset` e `sizes` centralmente derivados; o JSON dinâmico DEVE transportar a mesma projeção sanitizada. A página individual NÃO DEVE substituir a imagem editorial original por essas variantes. [2f061ba]
- `_sass/minimal-mistakes/skins/_variables-custom.scss` define tokens e animação de skeleton em CSS puro.
- `404.main.html` mantém implementação local equivalente para loader, imagem destacada e cards recentes, gerando `/404.html` em tempo de build.
- A implementação atual não grava EXIF nos arquivos originais porque a camada sidecar atende ao contrato com menor risco, sem nova dependência e sem mutação de assets autorais.

## Validação

- A validação visual DEVE comprovar controle sem deslocamento de layout, revelação por hover/foco e primeira interação, acionamento por mouse e teclado, proporção/contain na viewport, fechamento por botão e `Escape`, restauração de foco e ausência em imagens excluídas e impressão. [de992a9]
- A validação visual deve simular asset pesado pendente e confirmar que `.jcem-page-loaded` é aplicado antes de `document.readyState === "complete"`. [860dfdf]
- A validação visual deve confirmar que o conteúdo permanece visível antes, durante e após a inicialização dos aprimoramentos essenciais. [00251f5]
- A validação visual deve confirmar presença, geometria, pseudo-elemento e estado final dos skeletons em componentes elegíveis. [860dfdf]
- A validação visual deve aceitar skeleton pendente quando a animação estiver ativa e o asset correspondente ainda não estiver disponível. [860dfdf]
- A validação visual deve confirmar variação perceptível do skeleton entre dois momentos distintos de carregamento pendente. [860dfdf]
- A validação visual deve confirmar geometria e contraste mínimo operacional da barra superior de progresso. [860dfdf]
- `npm run check` deve validar o extrator de metadados em imagens reais do repositório. [860dfdf]
- O teste DEVE validar ordenação por largura, proporção homogênea, bytes, fallback, `srcset`/ `sizes` e seleção de variante menor em card estreito e DPR representativos. [860dfdf]
- Build Jekyll deve confirmar a geração de `assets/jcem/asset-metadata.json` e o uso opcional dos metadados no HTML renderizado. [860dfdf]
- A validação DEVE aferir separadamente hero legado, `content`, `wide single` e `wide triptych` em claro/escuro, orientações retrato/paisagem e viewports representativas; comprovar seleção, largura da zona aplicável, limites de altura, flag integral, centralização, proporção, crop natural do `wide single`, continuidade exclusiva do `triptych`, ausência de blur/superfície duplicada e ausência de vazamento entre modalidades. [32cb5f8]
- O hero legado DEVE ser exercitado em viewport no qual caiba integralmente e em viewport no qual alterne para a zona do artigo. O teste registra geometria antes e depois de resize, posição de scroll invariável, quantidade limitada de decisões e ausência de oscilação, flicker, cover parcialmente invisível ou escrita de layout em alta frequência. [32cb5f8]
- A impressão DEVE conservar a política vigente para imagens destacadas sem deixar superfície, segmento lateral, altura de tela ou sangria de uma modalidade interferir na composição impressa. [5db4d00]
- O build DEVE validar dimensões, tipo, hash/provenance e idempotência de derivados `1200×630`, `400×400` e WebP; segunda execução sem mudança de origem produz zero alteração de bytes e timestamp. [e983edf]
- O HTML final DEVE comprovar ordem e propriedades estruturadas de cada `og:image`, fallback square→wide, coerência de `twitter:card`/`twitter:image` e ausência de promessa ou tag proprietária inexistente por plataforma. [e983edf]
- Varredura de fontes publicáveis DEVE falhar diante de URL de asset em `web.archive.org`, sem reprovar `source:` histórico ou hyperlink citacional não incorporado. [e983edf]

Referência técnica: [Open Graph protocol — imagens estruturadas e arrays](https://ogp.me/).
