Estado operacional:
- Demanda em andamento em 2026-06-21: paridade do shell/noscript da 404, seis posts recentes via JSON assíncrono, compactação HTML conservadora e autoria opcional por post. Implementação concluída parcialmente; pendem build final, validação visual e revisão de paridade dos fragmentos.
- se a porta estiver ocupada, possível que `dev-live` já ativo pelo desenvolvedor
- AGENTS.md relido em 2026-06-13; ajustes aplicados em tema/validador visual.
- Validações concluídas: npm run check, npm run build:prod, npm run validate:visual.
- Menu/header ajustados em 2026-06-13: links imediatos do header alinhados à direita; menu aberto nítido acima do backdrop; conteúdo externo desfocado via backdrop.
- Ajustes visuais adicionais em 2026-06-13: ícones Font Awesome reexibidos, menu mobile dimensionado por conteúdo, controles flutuantes afastados da sobpostbar no fim da página.
- Header mobile em 2026-06-13: controles substitutos do menu alinhados à direita com logo preservado à esquerda; validação visual atualizada.
- Problema de ambiente: Browser interno falhou com CreateProcessAsUserW failed: 5; validação visual Playwright local executada com sucesso.
- GitHub Actions em 2026-06-13: npm ci falhava sem package-lock versionado; .gitignore ajustado para permitir package-lock.json e npm ci --dry-run validado.
- Posts em 2026-06-13: imagem destacada configurável por post e data por flag responsiva implementadas em layout/include/Sass; Browser interno ainda falha com CreateProcessAsUserW failed: 5.
- Post Devaneios em 2026-06-13: _posts/2014-04-16-devaneios.md criado de _drafts/devaneios/original.html com imagens locais, destaque wide, data preservada, 44 footnotes kramdown e validação Playwright local.
- Footnotes em 2026-06-13: marcadores inline ajustados sem borda/fundo/ícone externo; tooltip populado via TypeScript a partir das notas renderizadas; validações npm e Playwright local concluídas.
- Devaneios em 2026-06-13: Referências e Bibliografia separadas em blocos recolhíveis, notas abrem Referências automaticamente, colunas Markdown responsivas adicionadas e destaque wide reposicionado; build e validação visual concluídos.
- UI Devaneios em 2026-06-14: componentes de colunas/colapsáveis receberam aliases exportáveis, linhas amarelas e ícones sociais ajustados por tema, footnotes alinhados, logo extrapola a barra sem ampliar altura, e artigo wide recebeu fundo/fade conforme modo; build e validação visual concluídos.
- UI Devaneios em 2026-06-14: logo ajustado com slot fixo para extrapolar ~10% abaixo da masthead sem tocar o topo nem ampliar a barra; fade/crop wide invertido para transparente sobre a imagem e sólido no início do artigo; build e validação visual concluídos.
- UI Devaneios em 2026-06-14: logo realinhado sem ampliar masthead; crop wide reancorado ao cabeçalho do post com superfície clara sólida e degradê escuro de topo para transparente; build, validação visual geral e checagem Playwright desktop/reduced/mobile concluídos.
- UI Devaneios em 2026-06-14: flag SVG responsiva realinhada sem sobrepor título, overflow horizontal corrigido, blockquotes convertidos opcionalmente para painel futurista, compartilhamento suavizado; npm run check, build:prod e validate:visual concluídos.
- UI Devaneios em 2026-06-15: correção da flag preservando somente background SVG flagCinza/flagVermelho, título afastado por padding do header, header alinhado ao container visual do artigo e validação lateral 720px incluída; npm run check, build:prod e validate:visual concluídos.
- UI Devaneios em 2026-06-15: blockquotes substituídos por painel futurista com HTML setorizado por tabela conforme old-root, `painel.svg` aplicado nas quinas sem distorção e colunas reais `[10.5em|auto|6.5em]`; npm run check, build:prod e validate:visual concluídos.
- Impressão em 2026-06-15: mídia print força aliases do tema claro mesmo com tema escuro habilitado; validate-visual cobre `print` com `#jcem-theme-dark` ativo; npm run check, build:prod e validate:visual concluídos.
- Painel claro e impressão em 2026-06-15: `painel-modo-claro.svg` derivado de `painel.svg`, modo claro/print usam painel claro, imagem destacada e controles decorativos ocultos em print, colunas impressas forçadas para 1; build:prod e validate:visual concluídos.
- Cabeçalho Devaneios em 2026-06-15: cores do título e glitch suavizados por tema, link do título herda cor do título e tempo de leitura alinhado ao início visual do texto; build:prod, validate:visual e npm run check concluídos.
- Migração de posts em 2026-06-15: sete HTMLs de `_drafts` convertidos e publicados em `_posts` com data original preservada, `last_modified_at` 2026-06-15, imagens arquivadas, tabelas Markdown, blockquotes compatíveis com painel futurista e referências Kramdown conferidas.
- Validação de migração em 2026-06-15: `npm run check` passou; renderização direta Kramdown dos sete posts passou; integridade de footnotes passou sem referências ausentes e sem imagens relativas.
- Este projeto é codigificado em dois ambientes/equipamentos diferentes, um com playwright e oputro que apresenta(va) inconsitência playwright.
- Ambiente Ruby 4/Jekyll em 2026-06-15: scripts npm de build/dev passam por `scripts/jekyll_local.rb`, com compatibilidade local em `scripts/jekyll_compat.rb`; `npm run build:prod` passou no Ruby 4.0.5/Jekyll 4.4.1 local.
- Tabelas migradas em 2026-06-15: tabelas que eram emitidas como texto puro em `5-verdades-de-genesis-27`, `como-identificar-falacias` e `salvacao-importa-ser-perfeito` foram convertidas para HTML semântico; busca em `_site/p` não encontrou `<p>|`, `| --- |` nem HTML de tabela escapado.
- Imagem destacada em 2026-06-15: padrão `wide` removido; `wide` só ocorre quando explicitamente configurado por `featured_image_style`, `featured_image.style` ou aliases equivalentes.
- Referências/Bibliografia em 2026-06-15: `site.ts` recolhe seções manuais em `<details>` fechados, ignora sufixo `Permalink` nos headings e separa Bibliografia/Referências sem aninhamento indevido; include `jcem/components/references.html` também renderiza `<details>`.
- Blockquotes em 2026-06-15: padrão do layout permanece `blockquote_panels: true`; override global e por página documentado.
- Ambiente Playwright em 2026-06-15: `validate-visual.js` usa Chrome/Edge local quando o Chromium gerenciado não existe, ignora espera por fontes remotas em screenshot e mantém script de instalação por CDN oficial direto; `npm run validate:visual` passou com Chrome local.
- Página 404 em 2026-06-16: `404.html` estático criado no root sem front matter, herda `/assets/css/main.css`, usa preferência salva `jcem-theme` quando existir e, sem preferência, detecta o tema padrão publicado na home; fallback estático atual permanece dark por ser o padrão vigente; terminal possui paleta escura própria em dark/light e controles estilo Windows 11; `noscript`, `sobpostbar`, masthead sanitizado e `#footer` possuem fallback estático e hidratação por fetch da home quando JavaScript estiver disponível; masthead remove alternador de tema, busca e greedy-nav pesado na 404, com fallback local proporcional do logo em `assets/jcem/img/logo-animado.gif`; cabeçalho e footer preservam aparência do tema, incluindo Font Awesome da origem; `AGENTS.md` registra regras da 404, bloqueio de switch/Silktide/recursos com cookie ou `localStorage` e cuidado com porta `dev-live`; Playwright validou tema padrão, tema claro, terminal escuro, cabeçalho sem controles extras, logo proporcional e ausência de overflow; `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Loading global em 2026-06-16: `.carregandoPagina` exibido como unico elemento visivel ate `window.load`; `noscript` oculta o loading e mostra apenas o painel; `404.html` libera a pagina somente apos load e conclusao do gate de URL baseado em `R`; `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- 404 em 2026-06-16: apos descartar correspondencia em `R`, `404.html` define `document.title` como `404 - Página não encontrada - JeanCarloEm`; validate-visual cobre o titulo; `npm run build:prod` e `npm run validate:visual` passaram.
- Masthead sticky em 2026-06-17: `.masthead` alterada para `position: sticky; top: 0` em `assets/jcem/css/jcmain.scss`, cobrindo tema e `404.html` via `/assets/css/main.css`; `npm run check` e `npm run build:prod` passaram; validação alvo Playwright home/404 desktop/mobile confirmou fixação no topo ao rolar e retorno ao ponto original; `npm run validate:visual` excedeu 180s e deixou processos `node` 6064/15164 e `ruby` 11100/15624 ativos, sem encerramento por ausência de confirmação explícita.
- Masthead shadow e correção de blockquotes em 2026-06-17: sombra da masthead passou a depender da classe `jcem-masthead-stuck`, aplicada por TypeScript no tema e por script mínimo no `404.html`; sombra ausente no topo e presente somente quando sticky fixada, validada por Playwright em home/404 desktop/mobile e em tema claro/escuro. Artigo `_posts/2021-02-07-nenhum-nivel-de-alcool-faz-bem-a-saude.md` corrigido contra `_drafts/nenhum-nivel-de-alcool-faz-bem-a-saude/original.html`, removendo `>` de parágrafos autorais indevidamente incluídos em blockquote e mantendo as citações; varredura comparativa dos posts convertidos não encontrou outros parágrafos autorais confirmados dentro de blockquote. `npm run check` e `npm run build:prod` passaram.
- Regras editoriais de posts em 2026-06-17: `AGENTS.md` normatiza indentação visual de 4em na primeira linha de parágrafos comuns, bloqueia itálico automático em blockquotes/painéis de citação, exige itálico apenas em subcitações internas e em citações inline entre aspas no parágrafo comum.
- Formatação editorial aplicada aos posts publicados em 2026-06-18: `single.html` marca posts com `jcem-post`; Sass aplica indentação visual de 4em em parágrafos comuns e remove itálico automático de blockquotes/painéis; TypeScript marca citações inline entre aspas com `jcem-inline-quote`; `validate-visual.js` percorre todos os posts gerados em `_site/p/`. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Citações, footnotes e loading em 2026-06-18: `AGENTS.md` normatiza as regras; referências em blockquotes/painéis são normalizadas com `—`, linha própria, fonte menor e contexto ultrassintético quando reconhecido; `sup` de footnotes inline teve largura restringida para eliminar espaçamento excessivo; `carregandoPagina` recebeu progressbar fixa no topo com rastreio local de DOM/recursos e fallback equivalente na 404. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Cards de arquivo e footnotes em 2026-06-18: `_includes/archive-single.html` criado para renderizar imagem destacada, flag de data e link envolvendo todo o card; Sass aplica card claro/escuro com crop cover e wide quando configurado; `sup` de footnotes voltou a caixa inline compacta para eliminar espaçamento/encavalamento em texto justificado. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Ícones de link e proporção de cards em 2026-06-18: pseudo-ícone de links em parágrafos do artigo ficou inline, sem margem/padding que afaste do texto; cards comuns usam imagem em altura automática/proporção original; cards wide usam área 828:315 com crop central. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Listagem de publicações em 2026-06-18: grade de posts usa CSS puro com 1, 2 ou 3 colunas por largura de viewport; cards comuns não herdam `featured_image_style: wide` do artigo e preservam proporção real da imagem; crop wide fica reservado a estilo próprio de arquivo/card. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Imagem destacada da 404 em 2026-06-18: `404.html` recebeu imagem wide local `assets/jcem/img/e-o-fim-da-rosca-ccbysanc-jcem.png` com fundo definido por CSS local, altura limitada por clamp, escala vertical integral sem crop, centro fixo e crop permitido apenas no eixo horizontal; `.jcem-404` deixou de centralizar verticalmente no restante da viewport para impedir afastamento entre imagem e conteúdo. `scripts/validate-visual.js` valida imagem, fundo definido, centro, `object-fit: contain`, proporção natural, distância entre imagem e conteúdo e ausência de overflow horizontal na 404. `npm run check` e `npm run build:prod` passaram; validação Playwright direcionada da 404 passou em 1920/1366/1060x443/1055x1080/900/390.
- GitHub Actions visual em 2026-06-18: falha `Imagem destacada de card sem crop wide/cover em / dark wide` corrigida; Sass de cards reserva largura total mesmo com imagem externa ainda não carregada e preserva `height:auto`. `validate-visual.js` passou a validar cards normais por largura, limite de altura e proporção natural; 404 não fixa mais cor antiga `#171c26`, apenas exige fundo definido; validações responsivas ajustadas para `wide` e painéis aguardam estrutura final antes de medir. `npm run check`, `npm run build:prod` e `npm run validate:visual` passaram.
- Workflow Jekyll em 2026-06-18: `.github/workflows/jekyll.yml` recebeu cache de npm, Ruby, Playwright e renderização Jekyll (`_site`, `.jekyll-cache`, `.jekyll-metadata`, `.sass-cache`). Build incremental só ocorre com cache restaurado e diff seguro de assets estáticos; alterações estruturais, `_posts`, workflows, scripts, Sass, layouts, includes, config, dependências, páginas raiz ou deleções forçam build completo. Motivo: GitHub Pages exige artefato completo, não renderização completa; posts afetam home/feed/sitemap/paginação/relacionados e permanecem em build completo para evitar `_site` obsoleto. Validações: parse YAML via Ruby, `npm run check`, `git diff --check`.
- Coesão do `_site` em 2026-06-18: RCF/mapa de vínculos em `.github/jekyll-build-impact.yml`; scripts `scripts/jekyll_build_scope.rb` e `scripts/jekyll_build_manifest.rb` impedem mistura parcial entre `_site` local e cache do Actions. `_site` local só é aceito com manifesto `_site/.jcem-build-manifest.json` do commit atual e hashes válidos; caso contrário, `_site` do checkout é removido antes do cache. Cache incremental exige `.jekyll-cache/jcem-build-state.json` do commit base do diff; se ausente ou divergente, build completo. `validate-visual.js` deixou de validar contraste em contêiner estrutural `.main_jcem_wrapper`. Validações: `npm run check`, `npm run build:prod`, `npm run validate:visual`, parse YAML/Ruby.
- Timeout do workflow Jekyll em 2026-06-18: job `jekyll` limitado a 10 minutos; `scripts/ci_timed_step.rb` envolve comandos críticos com logs `[ci-time]` contendo timestamp, etapa, duração, status e timeout explícito (`exit 124`). `scripts/verify_root_deployment.rb` substitui verificação Bash frágil de CNAME e caminhos `/blog`. `.ci-timer-start` ignorado no Git. Validações: sintaxe Ruby, parse YAML, smoke de sucesso, smoke de timeout, `npm run check`, verificação de deploy raiz e `git diff --check`.
- Cards no Actions em 2026-06-18: falha `Imagem destacada de card sem crop wide/cover em / light side` corrigida removendo `max-height` de imagens normais de card, preservando proporção quando a imagem remota carrega no runner Linux. `validate-visual.js` mede imagem normal e wide separadamente e inclui métricas no erro. Validações: `npm run check`, `npm run build:prod`, `npm run validate:visual`.
- Validação visual no Actions em 2026-06-18: `validate-visual.js` permanece estrito por padrão para uso local/IA; no workflow Jekyll roda com `VISUAL_VALIDATION_STRICT=false`, gerando warning consultivo sem bloquear deploy. Instalação do browser Playwright também não bloqueia. Falso positivo de contraste em `.jcem-theme-toggle` removido da regra genérica por ser contêiner sem texto. Validações: parse YAML, `node --check`, modo consultivo simulado, modo estrito simulado, `npm run check`, `npm run build:prod`, `npm run validate:visual`.
- Fundo principal em 2026-06-19: `_variables-custom.scss` fixa o fundo temático no `.main_jcem_wrapper` com base sólida, `no-repeat` e `cover`; `404.html` deixou de reaplicar `var(--bg)` no bloco `.jcem-404`, mantendo apenas efeitos radiais transparentes para evitar corte horizontal/reinício do gradiente. `validate-visual.js` valida que a 404 não reinicia o fundo base. Validações: `npm run check`, `npm run build:prod`, `node --check scripts/validate-visual.js`, `git diff --check` passaram; `npm run validate:visual` excedeu 240s sem erro emitido, e validação Playwright direcionada passou em home, `/p/devaneios/` e 404 nos temas dark/light em desktop/mobile.
- Terminal da 404 em 2026-06-19: `404.html` passou a simular diagnóstico com variável `pagina`, concatenação de rota/arquivos, duas falhas MySQL, falha NoSQL, falha de API serverless localhost, busca Markdown, busca HTML e erro final destacado; animação de rolagem infinita feita em CSS puro com sequência duplicada e fallback `prefers-reduced-motion`. `validate-visual.js` valida conteúdo, animação CSS, duplicação `aria-hidden` e destaque final. Validações: `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod`, Playwright direcionado da 404 em dark/light desktop/mobile e `git diff --check` passaram.
- Terminal da 404 ajustado em 2026-06-19: variável `pagina` agora é lida de ambiente simulado (`REQUEST_SLUG=pagina`, `const pagina = env.REQUEST_SLUG`), sem atribuição literal; terminal usa `overflow:hidden`, scrollbar oculto, `white-space: normal`, linhas compactas e animação CSS sem pausa por hover/foco. Validações: `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod` e Playwright direcionado dark/light desktop/mobile confirmaram transform variando, scrollbar ausente e gap de linhas zero.
- Terminal da 404 em 2026-06-19: rolagem infinita corrigida para permanecer ativa mesmo com `prefers-reduced-motion: reduce`; somente cursor/glitch param nesse modo. Validações: `npm run check`, `npm run build:prod` e Playwright direcionado com `reducedMotion` em `no-preference` e `reduce` confirmaram `jcem-404-terminal-scroll` ativo e `transform` variando.
- Terminal da 404 em 2026-06-19: rolagem acelerada de 16s para 11s e efeito visual de digitação rápida adicionado via pseudo-elemento `.jcem-404__line::after` com `jcem-404-type-scan` em CSS puro. `validate-visual.js` valida duração da esteira e animação de digitação. Validações: `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod` e Playwright direcionado com `reducedMotion` em `no-preference` e `reduce`.
- Terminal da 404 em 2026-06-19: efeito de digitação refeito; scanner global removido, cursores HTML duplicados removidos, cursor passa a existir apenas na linha `.is-typing`. Rolagem trocada para controlador local mínimo que mede linhas e avança por uma linha com transição de 95ms a cada 230ms; tela do terminal ajustada para 8 linhas inteiras. Validações: `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod` e Playwright direcionado desktop/mobile com `reducedMotion` em `no-preference` e `reduce` confirmaram cursor único, salto de uma linha, `nowrap` e ausência de cursor embutido.
- Terminal da 404 em 2026-06-19: cursor sincronizado com largura real do texto por `Range`, ignorando linhas vazias e sem avançar por espaços finais; rolagem acelerada para passo de 205ms com transição de 85ms e digitação de 120ms; altura da tela reduzida para remover pico inferior da próxima linha. `validate-visual.js` passou a impedir cursor em linha vazia e avanço além da largura do texto. Validações: Browser interno indisponível (`iab`); Playwright local direcionado desktop/mobile com `reducedMotion` em `no-preference` e `reduce`, `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod`, `npm run validate:visual` e `git diff --check` passaram.
- Terminal da 404 em 2026-06-19: controlador deixou de usar `setInterval` e passou a sequenciar rolagem, espera da transição e digitação com `setTimeout`; cursor some durante a subida e só reaparece na nova linha. Cada digitação incrementa `data-jcem-terminal-tick`, e `validate-visual.js` falha se ticks consecutivos usarem a mesma linha. Tempos atuais: pausa 25ms, transição 75ms, digitação 105ms. Validações: `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod`, Playwright direcionado desktop/mobile com 5 ticks sem repetição em `no-preference` e `reduce`, `npm run validate:visual`.
- Terminal da 404 otimizado em 2026-06-19: digitação migrou de `requestAnimationFrame` para transições CSS sincronizadas de `left`; tampão e cursor compartilham duração e deslocamento. Fundo do terminal/tampão ficou sólido, grade e fades transparentes foram removidos. Passos, alvos e larguras são pré-calculados uma vez por carga/resize; avanço não faz varredura geométrica, resize possui debounce e animação pausa com documento oculto. Deduplicação compara texto visível inclusive na emenda do loop. `validate-visual.js` cobre runtime pré-calculado, fundo sólido e sincronismo. Validações: Browser interno indisponível (`iab`); `node --check scripts/validate-visual.js`, `npm run check`, `npm run build:prod`, Playwright local com ciclo completo de 24 ticks em desktop/mobile e `reducedMotion` nos dois estados, `npm run validate:visual`.
- Listagem e noscript em 2026-06-21: grade de publicações validada em CSS puro com 1-4 colunas; paginação Jekyll configurada para 8 posts; relacionados renderizam até 8 itens sem repetir o post atual; cards normais preservam proporção real e cards wide usam crop central apenas por estilo próprio de arquivo/card. `noscript` recebeu shell estático com masthead sem switcher, imagem `sem-motor-nao-vai-jcem-ccbysanc.png`, conteúdo reduzido e footer; 404 recebeu fallback equivalente local. Loader da página real aguarda cópia dos fragmentos estáticos do noscript antes de liberar a home. Validações: `npm run check`, `npm run build:prod`, `npm run validate:visual`.
- Noscript e cards em 2026-06-21: `noscript` passou a usar `.main_jcem_wrapper` próprio, sobpostbar, masthead e footer espelhados sem switcher/busca/storage, fundo padrão dark do blog, imagem wide em altura padrão com borda inferior e texto multilíngue anterior preservado com correções mínimas. Cards de arquivo liberam flag para fora do card, preservam ano, removem gap imagem/título, deixam título em altura natural e usam fallback de excerto. `validate-visual.js` cobre estes critérios. Validações: `npm run check`, `npm run build:prod`, `npm run validate:visual`, `git diff --check`.
- Noscript, 404 e card wide em 2026-06-21: faixa lateral removida evitando `100vw` em shell com scrollbar; `<noscript>` passa a rolar verticalmente com fundo do tema e figura em `#010203`; fallback noscript da 404 recebeu o mesmo ajuste. Terminal 404 sincroniza máscara/cursor no estado preparatório antes da digitação. Card wide usa altura do teaser, largura automática, centro fixo e overflow horizontal oculto, sem distorção. Validações: `node --check scripts/validate-visual.js`, `git diff --check`, `npm run check`, `npm run build:prod`, `npm run validate:visual`.
- Noscript, terminal 404 e Devaneios em 2026-06-21: fallback sem JavaScript deixou de ser overlay fixo e usa o scroll nativo do documento; regra do loader que bloqueava `body` foi neutralizada somente dentro de `<noscript>`; largura total e footer alcançável confirmados sem overflow horizontal; texto do painel foi reduzido em 15%. Terminal força tela, trilha, sequências e tampão para o mesmo fundo sólido e oculta a próxima linha durante preparação/digitação. Devaneios recebeu `archive_image_style: wide`; imagem 6345x460 mantém proporção natural, centro fixo e crop lateral. Validações: `npm run check`, `npm run build:prod`, medições Playwright direcionadas, revisão de screenshots, `npm run validate:visual` (`visual_validation=ok`).
